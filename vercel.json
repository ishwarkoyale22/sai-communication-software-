{
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace=apps/admin && rm -rf unified-dist && mkdir -p unified-dist && cp -r apps/admin/dist/. unified-dist/",
  "outputDirectory": "unified-dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
